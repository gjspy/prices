from typing import Generic, TypeVar, Type, Any, Optional, Literal, Union, overload, Self, Callable, Iterable, Coroutine

type DSA = dict[str, Any]
type DSS = dict[str, str]
type DSI = dict[str, int]
type QueryParams = tuple[Any, ...]